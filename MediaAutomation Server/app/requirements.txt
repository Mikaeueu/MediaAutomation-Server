# Web framework and ASGI server
fastapi==0.95.2
uvicorn[standard]==0.22.0

# Authentication / security
python-jose==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Scheduling and background jobs
APScheduler==3.10.1

# HTTP clients
httpx==0.24.1

# OBS websocket client
obs-websocket-py==0.5.3

# Google OAuth / YouTube (optional; required for OAuth flow and API calls)
google-auth==2.22.0
google-auth-oauthlib==1.0.0
google-api-python-client==2.86.0

# Cryptography for SecureStore
cryptography==41.0.3

# Type / data validation
pydantic==1.10.12
typing-extensions==4.8.0

# Utilities and tooling
python-dotenv==1.0.0

# Logging / observability (recommended)
loguru==0.7.0
sentry-sdk==1.24.0

# Production process manager
gunicorn==20.1.0

# Optional DB / migrations (only if you plan to persist data)
SQLAlchemy==2.0.49
alembic==1.11.1
